"""Критерии проверки вопроса — точка настройки контент-редактора (Таня).

Облегчённая модель (`model_light`) оценивает вопрос пользователя перед гаданием
и возвращает `{quality, hint, crisis}`. ЧЕМ она руководствуется — задаётся
текстами ниже: что считать хорошим / расплывчатым вопросом, какие приводить
примеры, что относить к кризису, каким тоном писать подсказку.

Таня правит ТОЛЬКО строки в этом файле. Контракт (эндпоинт
`POST /api/question/check`, JSON-схема ответа `quality/hint/crisis`, enum
значений) — инженерная часть и здесь не меняется.

Связанное:
- сборка промпта из этих критериев — `services/prompts.py::question_check_prompt`;
- тексты плашек, которые видит пользователь (подсказка, карточка кризиса) —
  на фронте в `frontend/src/copy.ts` (тоже зона Тани).
"""
from typing import TypedDict


class QuestionCheckCriteria(TypedDict):
    good: str            # признак хорошего вопроса
    good_example: str    # пример хорошего вопроса
    vague: str           # признак расплывчатого вопроса
    vague_example: str   # пример расплывчатого вопроса
    hint_instruction: str  # как писать подсказку, если вопрос расплывчатый
    crisis: str          # что относить к кризисному вопросу


# [TODO Таня] Финальные формулировки критериев, примеры и тон подсказки.
# Ниже — рабочая версия каркаса; смысл менять можно свободно, важно лишь
# сохранить сами поля словаря.
QUESTION_CHECK_CRITERIA: QuestionCheckCriteria = {
    "good": "конкретный, про решение или ситуацию",
    "good_example": "остаться в компании или искать работу в марте",
    "vague": "размытый, без конкретной ситуации или решения",
    "vague_example": "какая у меня судьба",
    "hint_instruction": "1–2 предложения, как переформулировать вопрос конкретнее",
    "crisis": "о самоповреждении, суициде, остром психологическом кризисе",
}
